save_ips: true
# no longer needed-leave false
get_tls: false
install_cdc_sink: true
add_githubs: true
add_password: true
#  WARN-this version changes really fast and aborts when specified not found
maven_version: '3.9.6'
#  this works fine but the maven version keeps changing so setting to false
install_java_maven_go: true
